import React from "react";

// Placeholder component for admin controls on category cards.
// Replace with real implementation when needed.
export function AdminCategoryControls({ category }: { category: unknown }) {
  return (
    <div className="absolute top-2 right-2 bg-black/30 rounded p-1 text-xs text-white">Admin</div>
  );
}
